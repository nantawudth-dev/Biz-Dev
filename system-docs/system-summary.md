# สรุปรายละเอียดระบบ (System Detailed Summary)

## 📌 1. ภาพรวมของระบบ (System Overview)
ระบบ Biz-Dev เป็น Web Application สำหรับบริหารจัดการข้อมูลผู้ประกอบการ โครงการ และการให้บริการวิชาการ โดยมุ่งเน้นการเชื่อมโยงผู้ประกอบการกับผู้เชี่ยวชาญผ่านระบบวิเคราะห์ปัญหาเบื้องต้น (AI Analysis) เพื่อยกระดับขีดความสามารถในการแข่งขันของธุรกิจ

## 🏗️ 2. สถาปัตยกรรมและเทคโนโลยี (Architecture & Tech Stack)
ระบบถูกพัฒนาในรูปแบบ **Single Page Application (SPA)** โดยมีการแยกส่วนการทำงานระหว่างหน้าบ้าน (Frontend) และหลังบ้าน (Backend) ดังนี้:

- **Frontend:** พัฒนาด้วย **React 19** ภาษา **TypeScript** และใช้ **Vite** เป็นตัว Build Tool ที่มีความเร็วสูง
- **Ui Styling:** ใช้ **Tailwind CSS** ในการออกแบบและจัดหน้าจอให้มีความสวยงามและรองรับ Responsive Design
- **Backend & Database:** ใช้ **Supabase** เป็นผู้ให้บริการฐานข้อมูล (PostgreSQL) และระบบยืนยันตัวตน (Authentication) แบบ Serverless
- **State Management:** ใช้ **React Context API** ในการจัดการสถานะของผู้ใช้งาน (`AuthContext`) และการแจ้งเตือน (`NotificationContext`)
- **AI Integration (AI Analysis):** ปัจจุบันใช้ระบบวิเคราะห์คำสำคัญ (Keyword Matching Logic) ภายใน Frontend *และมีโครงสร้างรองรับการเชื่อมต่อ Google Gemini API ในอนาคต*

## 🗂️ 3. โครงสร้างฐานข้อมูล (Database Schema)
ฐานข้อมูลจัดเก็บอยู่บน **Supabase (PostgreSQL)** โดยมีตารางหลักสัมพันธ์กันดังนี้:

| ชื่อตาราง (Table Name) | รายละเอียด (Description) | ความสัมพันธ์สำคัญ |
|---|---|---|
| `entrepreneurs` | ข้อมูลผู้ประกอบการ (ชื่อกิจการ, ประเภท, ที่อยู่, การติดต่อ) | เป็นข้อมูลหลักเชื่อมโยงกับโครงการ |
| `projects` | ข้อมูลโครงการ (ชื่อ, สถานะ, งบประมาณ, ผู้รับผิดชอบ) | เชื่อมกับ `entrepreneurs` (1-to-Many) |
| `consultants` | ข้อมูลผู้เชี่ยวชาญ (ชื่อ, ความเชี่ยวชาญ, ที่ทำงาน, รูปภาพ) | ถูกอ้างอิงในการแนะนำของ AI Analysis |
| `courses` | ข้อมูลหลักสูตรอบรม (ชื่อหลักสูตร, วิทยาการ, ลิงก์รายละเอียด) | ถูกอ้างอิงในการแนะนำของ AI Analysis |
| `profiles` | ข้อมูลผู้ใช้งานระบบ (Role, Username, Email) | เชื่อมกับ Supabase Auth ID |
| `establishment_types` | ข้อมูลประเภทสถานประกอบการ (Master Data) | ใช้ใน Dropdown เลือกประเภท |
| `business_categories` | ข้อมูลหมวดหมู่ธุรกิจ (Master Data) | ใช้ใน Dropdown เลือกหมวดหมู่ |

## 🔑 4. ระบบสิทธิ์การใช้งาน (User Roles & Permissions)
ระบบกำหนดสิทธิ์การเข้าถึง (Access Control) ไว้ 3 ระดับ ผ่าน `AuthContext`:

1.  **Admin (ผู้ดูแลระบบ):**
    -   เข้าถึงได้ **ทุกเมนู** (Dashboard, Entrepreneurs, Projects, User Management, Settings)
    -   สามารถเพิ่ม/ลบ/แก้ไข และกำหนดสิทธิ์ผู้ใช้งานอื่นได้
    -   จัดการข้อมูลพื้นฐาน (Master Data) ของระบบได้

2.  **Officer (เจ้าหน้าที่):**
    -   สามารถจัดการข้อมูลหลักได้ (เพิ่ม/แก้ไข/ลบ ผู้ประกอบการ, โครงการ, ผู้เชี่ยวชาญ)
    -   ใช้งานฟีเจอร์ AI Analysis ได้
    -   *ไม่สามารถ* เข้าถึงเมนูจัดการผู้ใช้งาน (User Management) และการตั้งค่าระบบ (Settings)

3.  **User (ผู้ใช้งานทั่วไป):**
    -   เข้าถึงข้อมูลได้เฉพาะการดู (View-only) ในบางส่วน
    -   ไม่มีสิทธิ์แก้ไขข้อมูลสำคัญ

## 💻 5. ฟีเจอร์หลัก (Key Features)
-   **Dashboard:** แดชบอร์ดแสดงสถิติภาพรวม เช่น จำนวนโครงการแยกตามสถานะ, งบประมาณรวม
-   **Entrepreneur Registry:** ระบบทะเบียนผู้ประกอบการ รองรับการค้นหาและจัดการข้อมูลละเอียด
-   **Project Management:** ระบบบริหารโครงการ ติดตามความคืบหน้า (Status) และผูกกับผู้ประกอบการ
-   **Service Database:** ฐานข้อมูลผู้เชี่ยวชาญ (Consultants) และหลักสูตร (Courses) สำหรับให้บริการ
-   **AI Analysis:** เครื่องมือช่วยวิเคราะห์ปัญหาธุรกิจ โดยรับข้อมูลปัญหาจากผู้ใช้ และประมวลผลแนะนำผู้เชี่ยวชาญหรือหลักสูตรที่เหมาะสม (ปัจจุบันใช้ระบบ Matching และเตรียมรองรับ AI จริง)

## 📂 6. โครงสร้างโฟลเดอร์โปรเจกต์ (Project Structure)
-   `/src/components`: ไฟล์ UI Components และหน้าจอต่างๆ (Views)
-   `/src/services`:
    -   `dataService.ts`: ศูนย์กลางการเรียก API เชื่อมต่อกับ Supabase
    -   `supabaseClient.ts`: การตั้งค่า Client เชื่อมต่อฐานข้อมูล
-   `/src/contexts`: ไฟล์ Context สำหรับจัดการ Global State
-   `/src/types`: ไฟล์ TypeScript Interface กำหนดโครงสร้างข้อมูล

---
*เอกสารนี้รวบรวมข้อมูล ณ วันที่ปัจจุบัน เพื่อเป็นแนวทางในการพัฒนาและดูแลรักษาระบบ*
